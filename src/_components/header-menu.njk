{% macro headerMenu(list, className) %}
    <nav class="header-nav">
        <ul class="header-nav-list {{ className }}">
            {% for item in list %}
                <li>
                    <a href="#{{item}}">{{ item }}</a>
                </li>
            {% endfor %}
        </ul>
    </nav>
{% endmacro %}